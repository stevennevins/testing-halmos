name: Test - Halmos

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  run-halmos:
    runs-on: ubuntu-latest
    steps:
      - name: Install halmos
        run: python3 -m pip install --upgrade halmos

      - name: Run halmos - check caller
        run: halmos
